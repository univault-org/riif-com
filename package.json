{
    "name": "markvault",
    "version": "1.0.0",
    "scripts": {
      "build": "node build.js",
      "serve": "npx serve dist",
      "dev": "npx serve site",
      "clean": "rm -rf dist"
    },
    "devDependencies": {
      "serve": "^14.2.1"
    }
  }